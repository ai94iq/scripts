#!/bin/bash
			#Add Keys/Repo's
			#Installing
			sudo apt update
			sudo apt-get install psensor fonts-roboto ttf-mscorefonts-installer telegram-desktop htop neofetch
			#Installing Snaps
			sudo snap install sublime-text	--classic
			sudo snap install vlc	--classic
			sudo snap install code	--classic
			sudo snap install spotify	--classic
exit
